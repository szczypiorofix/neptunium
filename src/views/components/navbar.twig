<nav class="navbar-container">
    <ul class="navbar">
        {% if navigationData %}
            {% for navigation in navigationData %}
                {% if navigation  %}
                <li>
                    <a href="{{ baseUrl ~ navigation.path }}">{{ navigation.title }}</a>
                </li>
                {% endif %}
            {% endfor %}
        {% endif %}
    </ul>
</nav>
